# harden-quality

A standalone, quality-only extraction of [`/harden quality`](../harden/) — same map-reduce harness and effort ladder, with all security/bugs content removed. Exists to evaluate model performance on pure maintainability auditing (named Fowler smells + analogs, blast-radius × change-frequency prioritization) in isolation.

**Temporary by design**: [`harden`](../harden/) is canonical; this skill self-documents that it should be deleted when the evaluation is done. Protocol in [`SKILL.md`](SKILL.md).
